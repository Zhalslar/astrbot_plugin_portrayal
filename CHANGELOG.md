# 更新日志

## v1.1.22

行为调整：

- `切换人格 <人格名或 ID>` 现在会**把机器人昵称改为该人格 ID**（与「切换人格 @群友」一致，
  一眼能看出当前在演谁）。切换前会照常备份机器人原始昵称，`恢复人格` 可还原。
- 该命令**不改动头像**：非群员克隆时没有来源头像可用，用 QQ 号去猜会把之前串掉的状态
  带回来；回执里会说明「头像未改动」。
- 切到本插件克隆的人格时，昵称会改成该克隆的 persona_id（`昵称_QQ号`）。

## v1.1.21
## v1.1.21

新功能：

- **`切换人格 <人格名或 ID>`**：切到**任意已有人格** —— AstrBot 自带的人格、其它插件
  建的人格、本插件克隆出来的人格都能切，不必是本群群员复刻的。
  - `切换人格 @群友` 保持原行为：切克隆人格并同步机器人昵称/头像
  - 按名字切换时**不改动**机器人昵称/头像（那些只属于群员克隆），并在回执里说明
  - 匹配顺序：精确 ID → 忽略大小写 → 唯一前缀 → 包含；有歧义时列出候选让你写全名
- **`人格列表`**：列出所有可切的人格（标注 `[克隆]` / `[自带]`）与切换用法

## v1.1.20
## v1.1.20

错误修复：

- **面板「未找到该路由」的真正原因**：宿主桥接会**自动补插件前缀**
  （`apiGet("stats")` → `/api/plug/<插件名>/stats`），而面板把插件名也写进了端点，
  于是请求变成 `/api/plug/astrbot_plugin_portrayal/astrbot_plugin_portrayal/overview`
  ——双重前缀，服务端匹配不到路由。
  现在端点统一使用**裸路径**（`overview` / `users` / `user/<uid>` / `update` /
  `generate` / `cached-users` / `diag`），桥接由宿主补前缀，直连由面板补前缀。

对照实验（同一接口三种路径）：
    /api/plug/<插件>/<插件>/overview    -> 未找到该路由（旧写法）
    /api/plug/<插件>/overview           -> 200 + 数据
    /api/v1/plugins/extensions/<插件>/overview -> 200 + 数据

## v1.1.19
## v1.1.19

错误修复：

- **修复「切换人格后昵称对了但头像不切换」**。原因是昵称设置后的**回读校验假阴性**：
  部分协议实现（如 NapCat）的 `get_login_info` 会返回缓存里的旧昵称，改名其实成功了却
  读回旧值，代码据此判定「昵称未生效」，进而**跳过了头像同步**。
  现在回读只作参考（会重试 3 次），不再据此判定失败；昵称与头像视为两件独立的事，
  任何一个出问题都不阻断另一个。

## v1.1.18
## v1.1.18

诊断：

- 改昵称/传头像会记录**协议端的原始返回**（`set_qq_profile` / `set_qq_avatar` 的
  status/retcode/message）。此前只看「有没有抛异常」，协议端把失败放在返回体里
  （例如改名次数用尽、参数不合法）时完全看不到原因。
- 协议端返回异常时，回执里直接带上 status/retcode/message。
- 新增 `查头像 @群友`：下载该群友头像并尝试设置，输出下载结果与协议端原始返回，
  用于确认「头像不切换」是网络下载问题还是协议端拒绝。

## v1.1.17
## v1.1.17

诊断与健壮性：

- 面板接口调用改为**以宿主桥接为主**（sandbox iframe 是不透明源，自己 fetch 会被
  CORS 拦掉，会显示 `Failed to fetch`），直连仅作兜底。
- 遇到「未找到该路由」时会自动换用另一种插件前缀（短名 / 作者名前缀）重试一次。
- 新增面板自检上报：面板启动、加载失败时会把「解析到的插件名、是否带 asset_token、
  实际错误」发到插件，写入 `panel_debug.log`（前缀 `FRONTEND`），便于远程定位。

## v1.1.16
## v1.1.16

错误修复：

- **面板「接口无响应（未找到该路由）」的真正原因**：从插件详情页进入时，宿主下发的
  插件标识是「作者/插件名」（例如 `zhalslar/astrbot_plugin_portrayal`），而插件接口
  注册用的是纯插件目录名，于是拼出的请求路径永远匹配不上——请求到了服务端，却没命中
  插件的处理函数（插件侧日志一条都不会有）。
  现在面板会**取标识的最后一段**，并从页面 URL 兜底解析插件名，保证路径正确。

## v1.1.15
## v1.1.15

错误修复：

- **面板「接口无响应 / 未找到该路由」**：面板改为**直连插件接口**（用页面自带的
  `asset_token` 作为 Bearer），不再依赖宿主桥接的端点映射。部分 AstrBot 版本的桥接
  只在本地登记部分端点，新增端点会在浏览器侧被判为「未找到该路由」，请求根本发不到
  插件（插件侧日志可证：一次调用都没有）。直连失败时仍会自动退回桥接。
- 登录态失效（401/403）时给出明确提示，而不是笼统的「接口无响应」。

## v1.1.14
## v1.1.14

诊断：

- 面板接口的每次调用都会记录到 `plugin_data/astrbot_plugin_portrayal/panel_debug.log`
  （只记路径与耗时，不含用户内容），用于定位「接口无响应 / 未找到该路由」时浏览器
  实际请求的 URL。

## v1.1.13
## v1.1.13

错误修复：

- **「改人格 … 重置:xxx」用半角冒号时会被当成 LLM 改写要求**。此前只认全角冒号「：」，
  写成 `重置:你是谁…` 时前缀不匹配，整段（含「重置:」二字）被当作「修改要求」交给 LLM
  重写，于是库里存下的是 LLM 的改写结果而不是你写的原文——表现为「我明明重置了，切过去
  还是另一份人格」。现在全角/半角冒号、空格、制表符都能识别；前缀写法可疑时会先提示用法
  并**拒绝执行**，不会再悄悄当成改写要求（避免白烧 token 还改坏人格）。
- 「查看克隆 @群友」现在会先告出该人格在 AstrBot 里的实际内容与本地是否一致，
  便于判断「切换到底生效没有」。

## v1.1.12

错误修复：

- 面板「未找到该路由」时不再整页作废：总览与列表**分别取数**，任一失败不影响另一部分
  渲染；顶部错误条写明失败原因并提供「重试」；接口在插件刚重载时短暂不可用会**自动退避重试**。
- 面板改为从宿主上下文读取运行期插件名，避免插件名与硬编码不一致时请求落到错误路由。
- 新增 `/ping` 自检接口（与其它面板接口同前缀），可在浏览器直接打开，用于确认路由是否可达。

## v1.1.11

错误修复：

- 修正上一版身份修复里的两个致命问题（都可能让「恢复人格」把**群友头像**当成机器人原图）：
  - **不再从当前账号反推原始头像**。账号头像一旦被换成群友头像，再去下载机器人 QQ 的头像拿到的就是那个群友的头像。现在原始头像只在「当前不是克隆状态」时采集一次，采集失败就记为「未知」，之后不会自动重试。
  - **没有确认过的原图就不动头像**，也不再谎报「头像已还原」。回执改为「已按备份原图提交还原（协议端未回执时请到 QQ 确认）」，缺原图时明确给出手动修复步骤。
- 「还原机器人资料」现在必须带「确认」二字，且在处于克隆状态时会拒绝执行，避免把群友头像存成机器人原图。
- 「查看机器人身份」读不到昵称时不再谎报「与备份一致」；会区分「昵称未知 / 头像未知 / 处于克隆状态」。
- 头像上传/昵称设置失败时不再登记「正在占用的昵称」，避免把没生效的名字当成克隆占用。
- 切换与恢复改用账号级串行锁，避免两个会话并发切换时互相覆盖昵称头像。

其他：

- `portrayal.json` 读取失败区分「内容损坏」与「IO 被占用」：IO 类失败不再改名、不再清空，并禁止本次写入，避免一次读取失败抹掉全部档案。
- 面板生成/改写人格时同步刷新画像时间，避免刚生成的档案被标成「画像早于人格改动」。
- 面板按纯数字 QQ 号过滤可建档候选。

## v1.1.10

错误修复：

- **修复「切换人格 / 恢复人格」之后机器人昵称、头像混乱的问题**。原因是「机器人原始昵称/头像」按会话（umo）保存，而 QQ 的昵称和头像是**账号级**的：A 会话切换后昵称变成群友 A，B 会话再切换时读到的「原始资料」其实已经是群友 A，于是一次次把克隆身份当成原始身份记住，最后还原时自然全错。
  - 原始资料改为**全局唯一一份**备份，存在插件数据目录的 `bot_identity.json`，只补空缺不覆盖；再也不会把克隆昵称记成真名。
  - 额外记录「我们推上去过的克隆昵称」，切换时若发现当前昵称正是克隆昵称且没有真名备份，会明确告警而不是继续记错。
  - **恢复人格不再拿 QQ 号拼接头像地址去顶**：那一刻协议端上挂着的还是克隆头像，`dst_uin` 地址会把克隆头像又设回去。现在优先用备份的原图字节，缺失时按机器人自己的 QQ 号重新下载，两者都不可用就明确提示需要手动恢复（不再谎报「头像已还原」）。
  - 头像改为「先自己下载再以 base64 上传」，并带备用头像源与图片类型校验；协议端从 URL 拉取失败时再退回 URL 方式。
  - 新增「查看机器人身份」（查看备份与当前状态、识别当前是否挂着克隆昵称）与「还原机器人资料」（把当前昵称/头像记为原始资料，用于备份丢失后人工修复）。

改进：

- 昵称设置后会回读校验，未生效会明确提示。
- 面板生成/改写会记录人格更新时间，画像早于当前人格时会标注「可能早于人格改动」。
- 面板可对「缓存里有聊天记录但还没建档」的群友一键建档。
- 面板「用缓存生成」现在会遵守消息缓存 TTL，不会拿过期记录生成人格。
- `portrayal.json` 改为「写临时文件 + 原子替换」，并在文件损坏时改名保留而不是当作空档案（避免一次坏文件把所有档案抹掉）。

## v1.1.9

新功能：

- 新增 WebUI 面板「人格面板」：在插件详情页点开即可一览所有群友档案与克隆人格
  （头像位、昵称、QQ 号、人格字数、画像字数、保护名单、最后更新时间），支持搜索、
  只看已克隆 / 只看有画像、按昵称 / 字数 / 时间排序与分页。
- 面板内可直接改人格：追加、整段替换、让 LLM 按修改要求重写；一键复制人格全文或
  「切换人格 @群友」命令；也能用本地缓存的聊天记录重新生成（有旧人格时自动融合）。
- 面板与 QQ 命令共用同一套业务逻辑（`core/persona_service.py`），两条入口的写入语义
  完全一致，写入即时落 `portrayal.json`。

改进：

- 修复「@机器人 改人格 @群友 …」这种调用方式会把机器人自己当成目标的问题
  （aiocqhttp 会把 `@bot` 留在消息链里）；现在会跳过自身 At。
- 「@全体成员」不再被当作目标用户，避免 `int("all")` 抛异常。
- 以目标 QQ 号开头的人格正文不再被误截断（只在「正好是 QQ 号」或「QQ号 + 空格」时剔除）。
- 内置命令（查看画像 / 查看克隆 / 改人格 / 切换人格 / 恢复人格）不再被提示词监听器
  重复处理，避免同名提示词条目导致双份回复与多余 LLM 调用。
- 拉取用户资料失败时给出提示而不是抛异常。

## v1.1.8

新功能：

- 克隆人格自动融合：当目标群友已存在克隆人格时，「克隆人格 @群友」会把旧人格与新抓取的聊天记录一起交给 LLM 融合完善，而不是整份重写；融合指令 `merge_prompt` 可在插件配置中自定义（清空后会自动回填内置默认文案）。
- 新增内置提示词命令「重克隆人格 @群友」：无视旧人格，走全新生成管线（权限与「克隆人格」一致，默认所有人可用）。
- 新增内置命令「改人格 @群友 <修改要求>」（仅管理员）：三种模式，`无前缀` 由 LLM 按修改要求重写（改写指令 `edit_prompt` 可自定义）、`追加：<补充文字>` 直接零成本追加到人格末尾、`重置：<完整人格>` 整段替换（目标用户无档案时自动拉取资料建档）。
- 新增内置命令「查看克隆 @群友」（仅管理员）：直接输出当前克隆人格全文，方便核对。

错误修复：

- 修复非克隆提示词命令（画像 / 正画像 / 负画像 / 找对象）会走人格融合分支、把人格文本写进画像字段的问题；现在这些命令只更新画像，不动克隆人格。
- LLM 返回空内容时不再写入数据库，避免把已有人格覆盖为空。

改进：

- 提示词模板渲染由 `str.format` 改为字面量替换，用户自定义提示词里出现花括号不再导致运行时异常。
- 命令词解析改用 `split()`，兼容全角空格与连续空格。
- 自动补齐内置提示词条目缺失的 `need_admin` 字段，插件加载时不再输出「缺少字段」告警。
- 「改人格」「查看克隆」会提示人格正文长度（超过 2000 字时提醒不建议直接群发全文）。

## v1.1.7

改进：

- 确保即使缓存中的消息数量少于配置的最大值，仍然返回已缓存的用户文本，从而避免不必要的重新扫描。

## v1.1.6

改进：

- 确保即使缓存中的消息数量少于配置的最大值，仍然返回已缓存的用户文本，从而避免不必要的重新扫描。

## v1.1.5

新功能：

    添加基于 JSON 的消息缓存存储，在多次运行之间保留每个用户的消息和群组游标。
    引入一个新的内置提示命令“找对象”，根据聊天记录推荐合适的对象类型。

错误修复：

    确保过期的群组缓存被完全清理并持久化到磁盘，避免重复使用过期消息。

改进：

    使用按群组划分的 asyncio 锁保护并发的群组历史扫描，以安全共享扫描进度。
    优化消息查询的元数据和文档字符串，使行为和意图更加清晰。
    将 portrayal 和 clone 命令的输出简化为纯文本响应，并移除对图像渲染的依赖。

## v1.1.4

新功能：

- 为每个提示词条目加上“仅管理员可用”的选项，增强对触发指令权限的控制
